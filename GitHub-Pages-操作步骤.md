# 让手机每天早上能看「每日简报」

有两条路，按需要选：

| | 方案 A：GitHub Pages（推荐） | 方案 B：局域网（应急） |
| --- | --- | --- |
| 手机上打开 | 公网网址，任何网络都能看 | 必须和电脑同一个 WiFi |
| 电脑要开着吗 | **不用**，云端每天自动更新 | 要，而且那个黑窗口不能关 |
| 要注册吗 | 要 GitHub 账号（免费） | 不用 |
| 一次性耗时 | 约 5 分钟 | 0 分钟 |

---

## 方案 A · GitHub Pages（早上自动更新，手机随时看）

> 原理：把站点放到 GitHub，每天北京时间 06:30 由 GitHub 的服务器抓新闻、重新生成页面并发布。
> 全程不需要你的电脑开机，也不需要 Codex 参与。

### 第 1 步 · 建一个空仓库（1 分钟）

1. 打开 https://github.com 登录（没账号先 Sign up）。
2. 右上角 **+** → **New repository**。
3. Repository name 填 `daily-brief`（会出现在网址里，可自取）；可见性选 **Public**。
4. **下面三个勾（README / .gitignore / license）一个都不要勾**，否则推送会冲突。
5. 点 **Create repository**，复制页面上的仓库地址，形如
   `https://github.com/你的用户名/daily-brief.git`。

### 第 2 步 · 双击 `推送到GitHub.cmd`（1 分钟）

这个脚本已经准备好，它会：

- 先抓一次最新新闻；
- 处理「目录属于沙箱账号」导致的 Git 所有权报错（`safe.directory`）；
- 初始化仓库、提交全部文件（仓库已经在 2026-09-13 预提交过一次，共 34 个文件）；
- 提示你粘贴第 1 步复制的仓库地址；
- 推送到 GitHub。首次会弹浏览器让你登录 GitHub，登录一次以后不再问。

### 第 3 步 · 打开 Pages（1 分钟，只需做一次）

1. 打开你的仓库 → **Settings** → 左侧 **Pages**；
2. **Source** 选 **GitHub Actions**（不要选 Deploy from a branch）；
3. 顶部 **Actions** → 左侧选「每日更新简报」→ 右侧 **Run workflow** 手动跑一次；
4. 等 1 分钟左右（出现绿勾），Pages 页面顶部就会给出网址：

```
https://你的用户名.github.io/daily-brief/
```

手机上打开它，**添加到主屏幕**（Safari：分享 → 添加到主屏幕；Chrome：⋮ → 添加到主屏幕），
以后早上点图标就能看，跟 App 一样。

### 之后每天会怎样

```
每天 06:30（北京时间）
  → GitHub 服务器抓新闻 → 生成页面 → 自动提交当天快照
  → 自动发布到 https://你的用户名.github.io/daily-brief/
```

每天的页面在 `site/daily/YYYY-MM-DD.html`，历史都在，可从「归档」翻。

> 提醒：GitHub 的定时任务在整点前后有几分钟到十几分钟的排队延迟，属于正常现象；
> 想立刻看最新，可以在 Actions 页面点 **Run workflow** 手动跑一次。

---

## 方案 B · 局域网秒看（不用注册）

双击 **`打开网站.cmd`**：

- 它会先抓最新新闻，再起一个本地网站服务；
- 窗口里会显示 `手机上打开： http://192.168.x.x:8080`；
- 手机连**同一个 WiFi**，浏览器输入这个地址即可；
- 窗口关掉网站就停，电脑关机也不能看。

适合「在家、电脑开着」的时候随手看，不适合早晨起床看。

---

## 常见问题

**推送失败说没登录？** 重新双击 `推送到GitHub.cmd`，按弹出的浏览器窗口完成授权。

**推送失败说 rejected / 冲突？** 建仓库时勾了 README 之类的初始文件。删掉仓库重建一个**空仓库**再推。

**网址打开是 404？** 检查两件事：① Actions 那次运行是否绿勾；② Settings → Pages 的 Source 是否选了 **GitHub Actions**。首次发布有时要等 1–3 分钟。

**想换更短的网址？** 仓库名取成 `你的用户名.github.io`，网址就是 `https://你的用户名.github.io/`。

**想让内容更好？** 现在源清单以 http:// 为主（本机 HTTPS 曾受限）。部署到云端后，GitHub 的服务器能正常访问 HTTPS，可以把源换成人民网、新华网、澎湃、36氪等，质量更高——需要就说一声。

**不想用命令行？** 也可以在空仓库页面点 **uploading an existing file**，把本文件夹**里面的内容**（不是文件夹本身）拖进去。注意先打开「查看 → 显示 → 隐藏的项目」把 `.github` 一起选上。
