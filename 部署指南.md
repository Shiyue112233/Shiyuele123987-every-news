# 把「每日简报」发布成真正的网站

> 现状：站点已经生成好了，在 `news\site\` 这个文件夹里（`index.html` 是首页）。
> 你要的"能用网址打开、不是 C 盘路径"，下面三条路任选。

---

## 方案一 · 本地网址（已经能用，不用注册任何东西）

双击 **`打开网站.cmd`**，会自动抓最新内容并起一个本地服务，浏览器打开：

```
http://localhost:8080
```

手机想一起看，就让手机和电脑连同一个 WiFi，用窗口里显示的局域网地址，形如：

```
http://192.168.x.x:8080
```

**优点**：零成本、零配置、内容不外传。
**限制**：只有这台电脑开着、这个窗口不关的时候能访问。

---

## 方案二 · Netlify Drop（3 分钟拿到公网网址，最省事）

1. 打开浏览器访问 **https://app.netlify.com/drop**
2. 把 `news\site` **整个文件夹**用鼠标拖进那个页面中间的上传区
3. 等几秒，页面上会出现一个公网地址，形如 `https://随机名字.netlify.app`
4. 想固定这个地址：注册/登录 Netlify（免费），在 Site settings 里把站点名改成你喜欢的，地址就变成
   `https://你的名字.netlify.app`

> **两个坑（实测）**
> 1. **未认领的站点会被自动加一道密码**，密码显示在认领页上（长得像一个灰色按钮，点一下可复制）。
> 2. **未认领的站点只存活 1 小时**，页面顶部有倒计时。想长期保留必须点「Claim this site」登录认领。
>
> 认领要登录账号；如果登录页出现 "complete the security check and try again"，
> 说明浏览器被判为自动化环境，换自己电脑上的 Chrome / Edge 重试即可（关掉 VPN / 代理）。
> 掉线也别慌，把 zip 重新拖一次就有新站点，认领链接会重新生成。

**以后每天更新**：重新跑一次 `更新日报.cmd`，再把 `site` 文件夹拖一次即可（Netlify 会覆盖更新）。

> 注意：Git 里不要放 `data\archive` 之外的隐私内容，本站只有公开新闻。

---

## 方案三 · GitHub Pages（一次配好，之后每天自动更新）

适合"我要它自己每天更新，不用我管"。

### 第一次配置

1. 在 GitHub 新建一个仓库，比如 `daily-brief`
2. 把 `outputs\新闻日报` 整个文件夹的内容推上去（保证仓库根目录下有 `scripts/`、`data/`、`site/`）
3. 把 `部署素材\github-actions-daily.yml` 复制到仓库的 `.github/workflows/daily.yml`
4. 仓库 Settings → Pages → Source 选 **GitHub Actions**
5. 手动跑一次 workflow（Actions → 每日更新简报 → Run workflow）

跑完你会得到 `https://你的用户名.github.io/daily-brief/` 这样的网址。

### 之后怎么运作

```
每天 UTC 22:30（北京时间 06:30）
  → GitHub 服务器拉取最新新闻 → 生成站点 → 自动提交并发布
```

**额外好处**：GitHub 的服务器能正常访问 HTTPS，所以到时候可以把源清单换成更全的源
（人民网、新华网、36氪、澎湃等），覆盖面和新鲜度都会比现在好。需要的话我改一版 `sources.json`。

---

## 附：想用自己的域名？

- Netlify：Site settings → Domain management → Add custom domain，按提示在域名商处加一条 CNAME。
- GitHub Pages：仓库 Settings → Pages → Custom domain。
- 国内服务器要备案，海外域名商（Namesilo、Cloudflare）不用。

---

## 附：三个方案怎么选

| 你的需求 | 选哪个 |
| --- | --- |
| 就想自己看着方便，不想折腾 | 方案一 |
| 想有个网址能发给别人 / 手机上随时看 | 方案二 |
| 想"配一次，之后自动每天更新" | 方案三 |

---

## 如果你希望我来操作

我可以直接在你的浏览器里完成方案二的上传（或者帮你把仓库和 workflow 建好跑通），
但其中涉及登录你的账号，需要你在旁边点一下登录/授权。想做的话跟我说一声就行。
